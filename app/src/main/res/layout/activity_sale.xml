<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/rootSale"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@drawable/bg_soft_gradient">

    <!-- Header -->
    <LinearLayout
        android:id="@+id/header"
        android:layout_width="match_parent"
        android:layout_height="56dp"
        android:orientation="horizontal"
        android:gravity="center_vertical"
        android:paddingHorizontal="12dp">

        <ImageButton
            android:id="@+id/btnClose"
            android:layout_width="36dp"
            android:layout_height="36dp"
            android:padding="8dp"
            android:background="?attr/selectableItemBackgroundBorderless"
            android:src="@drawable/ic_close_24" />

        <TextView
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:text="Bán hàng"
            android:textStyle="bold"
            android:textSize="22sp"
            android:gravity="start" />

        <TextView
            android:id="@+id/btnDone"
            android:layout_width="wrap_content"
            android:layout_height="32dp"
            android:paddingHorizontal="24dp"
            android:gravity="center"
            android:text="Xong"
            android:textSize="18sp"
            android:textStyle="bold"
            android:textColor="#f2f2f2"
            android:background="@drawable/bg_pill_soft" />
    </LinearLayout>

    <!-- Ô chọn khách hàng -->
    <LinearLayout
        android:id="@+id/rowCustomer"
        android:layout_below="@id/header"
        android:layout_margin="16dp"
        android:layout_width="match_parent"
        android:layout_height="56dp"
        android:background="@drawable/bg_card_16"
        android:gravity="center_vertical"
        android:paddingHorizontal="16dp"
        android:clickable="true"
        android:focusable="true">

        <TextView
            android:id="@+id/tvCustomer"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:gravity="center_vertical"
            android:text="Khách hàng, phòng bàn..."
            android:textColor="#8f8f8f"
            android:textSize="16sp" />
    </LinearLayout>

    <LinearLayout
        android:id="@+id/contentGuide"
        android:layout_below="@id/rowCustomer"
        android:layout_above="@id/quickBar"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:orientation="vertical"
        android:gravity="center"
        android:padding="24dp"
        android:visibility="visible"> <TextView
        android:id="@+id/tvTitle"
        android:text="Đơn này bạn bán hàng gì?"
        android:textStyle="bold"
        android:textSize="18sp"
        android:textColor="#212B36"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="24dp"/>

        <TextView
            android:id="@+id/tvSub"
            android:text="Chat tên hàng hoặc đọc tên hàng để VoiceNote tính tiền nhanh."
            android:textColor="#6B7280"
            android:gravity="center"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginTop="8dp"/>
    </LinearLayout>


    <androidx.core.widget.NestedScrollView
        android:id="@+id/cartScrollView"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_below="@id/rowCustomer"
        android:layout_above="@id/quickBar"
        android:clipToPadding="false"
        android:visibility="gone"
        tools:visibility="visible">

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="vertical"
            android:paddingTop="8dp">

            <androidx.recyclerview.widget.RecyclerView
                android:id="@+id/rvOrderLines"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:paddingHorizontal="16dp" /><LinearLayout
            android:id="@+id/totalBar"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:paddingHorizontal="20dp"
            android:paddingVertical="12dp"
            android:gravity="center_vertical">

            <TextView
                android:layout_width="0dp"
                android:layout_height="wrap_content"
                android:layout_weight="1"
                android:text="Tổng cộng"
                android:textStyle="bold"
                android:textSize="18sp"
                android:textColor="#0F172A"/>

            <TextView
                android:id="@+id/tvTotal"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                tools:text="80,000"
                android:textStyle="bold"
                android:textSize="20sp"
                android:textColor="#0F172A"/>
        </LinearLayout>

        </LinearLayout>
    </androidx.core.widget.NestedScrollView>

    <!-- ⚡ QUICK BAR -->
    <LinearLayout
        android:id="@+id/quickBar"
        android:layout_alignParentBottom="true" android:layout_width="match_parent"
        android:layout_height="68dp"
        android:background="#F7F8FA"
        android:padding="12dp"
        android:elevation="8dp"
        android:gravity="center_vertical">

        <ImageButton
            android:id="@+id/btnGrid"
            android:layout_width="44dp"
            android:layout_height="44dp"
            android:background="@drawable/bg_circle_soft"
            android:src="@drawable/ic_grid_24"
            android:contentDescription="grid button"/>

        <EditText
            android:id="@+id/edtLine"
            android:layout_width="0dp"
            android:layout_height="48dp"
            android:layout_weight="1"
            android:layout_marginHorizontal="12dp"
            android:paddingStart="16dp"
            android:paddingEnd="16dp"
            android:hint="Nhập tên hàng + giá"
            android:background="@drawable/bg_input_pill"/>

        <ImageButton
            android:id="@+id/btnMic"
            android:layout_width="44dp"
            android:layout_height="44dp"
            android:background="@drawable/bg_circle_soft"
            android:src="@drawable/ic_mic_24"
            android:contentDescription="mic button"/>
    </LinearLayout>

    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/rvQuickGrid"
        android:layout_alignParentBottom="true" android:layout_width="match_parent"
        android:layout_height="220dp"
        android:visibility="gone" android:background="@android:color/white"
        android:elevation="4dp"
        android:padding="12dp"
        android:clipToPadding="false"/>
</RelativeLayout>
